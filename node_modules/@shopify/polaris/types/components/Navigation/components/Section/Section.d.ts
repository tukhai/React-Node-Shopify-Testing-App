import React from 'react';
import { IconProps } from '../../../../types';
import { ItemProps } from '../Item';
export interface SectionProps {
    items: ItemProps[];
    icon?: IconProps['source'];
    title?: string;
    fill?: boolean;
    rollup?: {
        after: number;
        view: string;
        hide: string;
        activePath: string;
    };
    action?: {
        icon: IconProps['source'];
        accessibilityLabel: string;
        onClick(): void;
    };
    separator?: boolean;
}
interface State {
    expanded: boolean;
}
export declare class Section extends React.Component<SectionProps, State> {
    state: State;
    private animationFrame;
    componentWillUnmount(): void;
    render(): JSX.Element;
    private handleClick;
    private toggleViewAll;
}
export {};
