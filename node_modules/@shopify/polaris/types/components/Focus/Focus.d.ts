import React from 'react';
export interface FocusProps {
    children?: React.ReactNode;
    disabled?: boolean;
    root: HTMLElement | null;
}
export declare class Focus extends React.PureComponent<FocusProps, never> {
    componentDidMount(): void;
    componentDidUpdate({ children: prevChildren, ...restPrevProps }: FocusProps): void;
    handleSelfFocus(): void;
    render(): JSX.Element;
}
