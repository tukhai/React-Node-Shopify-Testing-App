/// <reference types="react" />
export interface TagProps {
    /** Content to display in the tag */
    children?: string;
    /** Disables the tag  */
    disabled?: boolean;
    /** Callback when tag is removed */
    onRemove?(): void;
}
export declare function Tag({ children, disabled, onRemove }: TagProps): JSX.Element;
