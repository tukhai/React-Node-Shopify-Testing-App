import React from 'react';
export interface TrapFocusProps {
    trapping?: boolean;
    children?: React.ReactNode;
}
interface State {
    shouldFocusSelf: boolean | undefined;
}
export declare class TrapFocus extends React.PureComponent<TrapFocusProps, State> {
    state: State;
    private focusTrapWrapper;
    componentDidMount(): void;
    handleTrappingChange(): {
        shouldFocusSelf: boolean;
    };
    render(): JSX.Element;
    private readonly shouldDisable;
    private setFocusTrapWrapper;
    private handleBlur;
}
export {};
