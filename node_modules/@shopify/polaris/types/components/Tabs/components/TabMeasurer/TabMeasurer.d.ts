import React from 'react';
import { TabDescriptor } from '../../types';
export interface TabMeasurements {
    containerWidth: number;
    disclosureWidth: number;
    hiddenTabWidths: number[];
}
export interface TabMeasurerProps {
    tabToFocus: number;
    siblingTabHasFocus: boolean;
    activator: React.ReactElement<{}>;
    selected: number;
    tabs: TabDescriptor[];
    handleMeasurement(measurements: TabMeasurements): void;
}
export declare class TabMeasurer extends React.PureComponent<TabMeasurerProps, never> {
    private containerNode;
    private animationFrame;
    componentDidMount(): void;
    componentDidUpdate(prevProps: TabMeasurerProps): void;
    render(): JSX.Element;
    private handleMeasurement;
}
