import React from 'react';
import { Theme } from '../../utilities/theme/types';
interface State {
    theme: Theme;
    colors: string[][] | undefined;
}
interface ThemeProviderProps {
    /** Custom logos and colors provided to select components */
    theme: Theme;
    /** The content to display */
    children?: React.ReactNode;
}
export declare class ThemeProvider extends React.Component<ThemeProviderProps, State> {
    state: State;
    componentDidUpdate({ theme: prevTheme }: ThemeProviderProps): void;
    render(): JSX.Element;
    createStyles(): {} | null;
}
export {};
